# Approval checklist

Use this before any browser-assisted posting.

## Account state

- [ ] LinkedIn is logged in.
- [ ] X is logged in.
- [ ] Correct account is active on LinkedIn.
- [ ] Correct account is active on X.

## Publish approval

Before I click any final publish/post/reply/comment/send button, user approval is required.

## Current handoff state

- X thread is already live.
- X link reply is already live.
- LinkedIn anchor is already live: `https://www.linkedin.com/feed/update/urn:li:activity:7474074950476382208/`.
- LinkedIn first comment is already live with the LinkedIn UTM essay URL.
- User approved all pending live actions on 2026-06-20.
- X engagement Batch A has been posted.
- Direct outreach Batch 1 is partially sent as public touches.
- LinkedIn carousel is approved but blocked in the in-app browser because LinkedIn did not expose a browser-accessible PDF upload input.

### LinkedIn anchor post

Approve this exact action:

- Paste `copy-ready/linkedin-anchor-post.txt` into LinkedIn composer.
- Publish as a public LinkedIn post.
- Add `copy-ready/linkedin-first-comment.txt` as the first comment.

Approval phrase:

`approved: publish LinkedIn anchor`

Status: already used.

### LinkedIn document carousel

Approve this exact action:

- Upload `assets/carousel/first-ten-customers-carousel.pdf` to LinkedIn as a document post.
- Paste `copy-ready/linkedin-carousel-caption.txt` as the post caption.
- Publish as a public LinkedIn document/carousel post.
- Add `copy-ready/linkedin-carousel-first-comment.txt` as the first comment.

Approval phrase:

`approved: publish LinkedIn carousel`

Status: approved, but not published. Browser-assisted publishing reached the caption/upload step, then blocked on file upload automation.

### X thread

Approve this exact action:

- Paste `copy-ready/x-thread.txt` into X as a thread.
- Publish the thread.
- Add `copy-ready/x-link-reply.txt` as the link reply.

Approval phrase:

`approved: publish X thread`

Status: already used.

### Pre-launch comments/replies

Approve this exact action:

- Use `search-queues.md` to find relevant posts.
- Draft comments/replies from the prompt bank.
- Stop before posting each comment unless user approves comment posting in batch.

Approval phrase for batch:

`approved: post up to 5 LinkedIn comments and 10 X replies from the approved prompt bank`

### Post-launch X engagement batch

Approve this exact action:

- Use `x-engagement-queue-2026-06-20.md`.
- Inspect each target post immediately before replying.
- Post Batch A replies if the target context is still relevant.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: post X engagement batch A`

Status: already used for Batch A.

### Reply to Luke Sophinos

Approve this exact action:

- Open Luke Sophinos's reply: `https://x.com/lukesophinos/status/2068384975523643431`.
- Paste `copy-ready/x-reply-luke-sophinos-horizontal-gpt.txt`.
- Publish the reply from Ruchit's X account.
- Update `reply-source-scorecard.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: reply to Luke Sophinos`

Status: drafted only, not posted.

### Horizontal GPT X follow-up

Approve this exact action:

- Publish `copy-ready/x-horizontal-gpt-followup.txt` as a standalone X post from Ruchit's X account.
- Do not include the essay link in the main post.
- Add the essay link as a reply only if the post gets engagement or the user explicitly approves a link reply.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: publish horizontal GPT X follow-up`

Status: drafted only, not posted.

### Horizontal GPT X link reply

Approve this exact action:

- Add `copy-ready/x-horizontal-gpt-link-reply.txt` as a reply under the horizontal GPT X follow-up.
- Use only after the horizontal GPT X follow-up is live and has early engagement, unless the user explicitly wants the link reply immediately.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: add horizontal GPT X link reply`

Status: drafted only, not posted.

### Horizontal GPT X engagement queue

Approve this exact action:

- Use `horizontal-gpt-engagement-queue-2026-06-21.md`.
- Inspect each X target live before replying.
- Post only context-fit, no-link replies from the approved queue.
- Skip any target that is stale, spammy, political, or unrelated.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: post horizontal GPT X engagement queue`

Status: drafted only, not posted.

### Horizontal GPT LinkedIn follow-up

Approve this exact action:

- Publish `copy-ready/linkedin-horizontal-gpt-followup.txt` as a standalone LinkedIn post from Ruchit's LinkedIn account.
- Do not include the essay link in the main post.
- Add the essay link as a first comment only if the user explicitly approves the link comment.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: publish horizontal GPT LinkedIn follow-up`

Status: drafted only, not posted.

### Horizontal GPT LinkedIn first comment

Approve this exact action:

- Add `copy-ready/linkedin-horizontal-gpt-first-comment.txt` as the first comment under the horizontal GPT LinkedIn follow-up.
- Use only after the LinkedIn horizontal GPT follow-up is live.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: add horizontal GPT LinkedIn first comment`

Status: drafted only, not posted.

### LinkedIn horizontal GPT engagement queue

Approve this exact action:

- Use `linkedin-horizontal-gpt-engagement-queue-2026-06-21.md`.
- Inspect each LinkedIn target live before commenting.
- Post only context-fit, no-link comments from the approved queue.
- Skip any target without a clear current post, visible comment path, or relevant context.
- Update `tracker.csv`, `metrics-log.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: post LinkedIn horizontal GPT engagement queue`

Status: drafted only, not posted.

### High-engagement room scouting

Approve this exact action:

- Use `high-engagement-commenting-playbook.md`.
- Search LinkedIn and X for active founder/operator/investor posts.
- Draft specific comments/replies only.
- Do not post, reply, comment, DM, or send anything.

Approval phrase:

`approved: scout high-engagement rooms`

Status: drafted only, not executed.

### LinkedIn high-engagement comment batch

Approve this exact action:

- Use `linkedin-high-engagement-comment-batch-2026-06-22.md`.
- Inspect each LinkedIn target live before commenting.
- Post only no-link comments under posts that still have relevant active engagement.
- Skip any target without a clear current post, visible comment path, or topic fit.
- Update `tracker.csv`, `metrics-log.csv`, `reply-source-scorecard.csv` if needed, and `WORKLOG.md`.

Approval phrase:

`approved: post LinkedIn high-engagement comment batch`

Status: drafted only, not posted.

### X high-engagement reply batch

Approve this exact action:

- Use `x-high-engagement-reply-batch-2026-06-22.md`.
- Inspect each X target live before replying.
- Post only no-link replies under posts that still have relevant active engagement.
- Skip stale, spammy, political, or unrelated targets.
- Update `tracker.csv`, `metrics-log.csv`, `reply-source-scorecard.csv` if needed, and `WORKLOG.md`.

Approval phrase:

`approved: post X high-engagement reply batch`

Status: drafted only, not posted.

### Direct outreach

Approve this exact action:

- Use `direct-outreach-batch-1-messages.md`.
- Verify each recipient/profile/recent post before sending.
- Stop before sending each DM/email unless user approves a specific batch.
- Update `direct-outreach-tracker.csv`, `tracker.csv`, and `WORKLOG.md`.

Approval phrase for batch:

`approved: send direct outreach batch 1`

Status: already used for public-touch Batch 1; partial completion is recorded in `direct-outreach-tracker.csv`.

### Direct outreach Batch 2

Approve this exact action:

- Use `direct-outreach-batch-2-messages.md`.
- Verify each recipient/profile/recent post before sending.
- Prefer a public comment/reply if there is a current relevant post.
- Stop before each DM/email unless user approves the specific batch.
- Update `direct-outreach-tracker.csv`, `reply-source-scorecard.csv`, `tracker.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: send direct outreach batch 2`

Status: drafted only, not sent.

### Direct outreach Batch 3

Approve this exact action:

- Use `direct-outreach-batch-3-messages.md`.
- Verify each recipient/profile/recent post before sending.
- Prefer a public comment/reply if there is a current relevant post.
- Stop before each DM/email unless user approves the specific batch.
- Update `direct-outreach-tracker.csv`, `reply-source-scorecard.csv`, `tracker.csv`, and `WORKLOG.md`.

Approval phrase:

`approved: send direct outreach batch 3`

Status: drafted only, not sent.

## Safety rules

- No mass spam.
- No fake engagement.
- No automated posting without approval.
- No comments that pretend to have personal context we did not verify.
- No DMs/emails without a specific reason the essay helps the recipient.

## After publishing

Immediately update:

- `tracker.csv` with post URLs.
- `metrics-log.csv` at T+1h, T+3h, T+6h, T+24h, T+48h.
- `WORKLOG.md` with what was posted and what remains.
