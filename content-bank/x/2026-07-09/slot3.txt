Keep a do-not-regress file: the 10 worst outputs you ever shipped, plus what the fix looked like. Every new model reruns against those 10 before anything else. Cheaper than a benchmark, and it tests the failures you actually paid for.
