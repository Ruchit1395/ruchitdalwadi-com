A prompt fixes one output. A system fixes the category: default context, examples the model copies, checks before anything ships. Retyping the same clever prompt every week is a habit, not infrastructure.
