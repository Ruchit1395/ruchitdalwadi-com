PMs on AI features: write the eval before the spec. Three outputs you would ship, three you would reject, one mistake that is unacceptable. It fits on a page, and it forces the "what is this for?" conversation before engineering builds the wrong thing fast.
