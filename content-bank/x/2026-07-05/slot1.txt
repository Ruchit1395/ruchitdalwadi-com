Write the task as a numbered list before you build the agent. If the list is complete and the order never changes, you need a workflow with one LLM call, not an agent. Agents are for paths you cannot enumerate upfront.
